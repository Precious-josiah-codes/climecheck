const Loader = () => {
  return (
    <div className="fixed w-full h-full bg-[black] flex justify-center items-center">
      <div className="text-[1.5rem] sm:text-[3rem] font-bold text-tetiary animate-pulse">
        CLIMECHECK
      </div>
    </div>
  );
};

export default Loader;
